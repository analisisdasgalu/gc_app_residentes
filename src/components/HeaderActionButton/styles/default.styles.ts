import { colors } from "@gcMobile/theme/default.styles";
import { ViewStyle } from "react-native";

export const headerActionStyles: ViewStyle = {
	width: "20%",
	padding: 5,
	flexDirection: "row",
	justifyContent: "center",
	borderRadius: 5,
};
