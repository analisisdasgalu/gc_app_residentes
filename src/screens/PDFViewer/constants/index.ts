import { ViewStyle } from 'react-native'

export const containerStyles: ViewStyle = {
    width: '100%',
    height: '100%',
    backgroundColor: 'white',
}

export const pdfViewerStyles: ViewStyle = {
    width: '100%',
    height: '100%',
    backgroundColor: 'white',
}
